import { governmentContributionSettings } from '../services/benefitsService';
export function GovernmentContributionsPage() { return <section><h2>PH Government Contribution Setup</h2><p className="lead">Configurable placeholders only; no statutory rates are hardcoded.</p><div className="cards">{governmentContributionSettings.map((setting) => <article className="record-card" key={setting.id}><h3>{setting.agencyName}</h3><p>Employee formula: {setting.employeeFormulaCode}</p><p>Employer formula: {setting.employerFormulaCode}</p><p>Active: {String(setting.active)}</p></article>)}</div></section>; }
