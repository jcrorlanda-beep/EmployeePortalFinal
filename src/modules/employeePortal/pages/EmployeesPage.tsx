import { useEffect, useState } from 'react';
import { employeeService } from '../services/employeeService';
import { createAuditMetadata } from '../services/employeePortalApi';
import type { Employee } from '../types/employeeTypes';
export function EmployeesPage() { const [employees, setEmployees] = useState<Employee[]>([]); useEffect(() => { void employeeService.listEmployees().then(setEmployees); }, []); const addEmployee = async () => { const created = await employeeService.createEmployee({ employeeNumber: `NCCC-${String(employees.length + 1).padStart(3, '0')}`, firstName: 'New', lastName: 'Employee', departmentId: 'dept_shop', positionId: 'pos_technician', status: 'onboarding', hireDate: '2026-05-06' }, createAuditMetadata('mvp-admin', 'MVP employee CRUD create')); setEmployees([created, ...employees]); }; return <section><h2>Employee Management</h2><p className="lead">Create and review employee records without localStorage or TalyerOS auth integration.</p><button className="primary" onClick={addEmployee}>Add mock-safe employee</button><div className="table-card"><table><thead><tr><th>No.</th><th>Name</th><th>Status</th><th>Department</th><th>Position</th></tr></thead><tbody>{employees.map((employee) => <tr key={employee.id}><td>{employee.employeeNumber}</td><td>{employee.firstName} {employee.lastName}</td><td>{employee.status}</td><td>{employee.departmentId}</td><td>{employee.positionId}</td></tr>)}</tbody></table></div></section>; }
