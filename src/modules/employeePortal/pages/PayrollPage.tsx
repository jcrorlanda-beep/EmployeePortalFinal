import { payrollPeriods, payrollProfiles } from '../services/payrollService';
import { payrollFormulaSafetyChecklist } from '../utils/payrollFormulaUtils';
export function PayrollPage() { return <section><h2>Payroll Preparation</h2><p className="lead">Preparation only. Payroll finalization is intentionally not implemented.</p><ul className="checklist">{payrollFormulaSafetyChecklist.map((item) => <li key={item}>{item}</li>)}</ul><div className="cards">{payrollProfiles.map((profile) => <article className="record-card" key={profile.id}><h3>{profile.employeeId}</h3><p>Pay type: {profile.payType}</p><p>Base formula: {profile.baseFormulaCode}</p></article>)}{payrollPeriods.map((period) => <article className="record-card" key={period.id}><h3>{period.name}</h3><p>{period.startsOn} to {period.endsOn}</p><p>Status: {period.status}</p></article>)}</div></section>; }
